const todo = [{
    text: 'order cast food',
    completed: true
},{
    text:'Clean kitchen',
    completed: false
}]
//const incompletTodos = todo.filter(function(todo){
  //  return  !todo.completed
//})
//const newParagraph = document.createElement('p')
//newParagraph.textContent = `you have ${incompletTodos.length} todos lenght`
//document.querySelector('body').appendChild(newParagraph)

/*const ps = document.querySelectorAll('p')
ps.forEach(function(a)
{
    if(a.textContent.includes('the'))
 {
     a.remove()
 }
  
})*/
document.querySelector('button').addEventListener('click',function(e){
console.log("heyyy")
})